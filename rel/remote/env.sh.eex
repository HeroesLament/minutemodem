#!/bin/sh

# Remote UI release: connects to a core node
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=ui@${HOSTNAME:-localhost}

# The core node to connect to (override at runtime)
export MM_CORE_NODE="${MM_CORE_NODE:-core@localhost}"
